# WorkPack 项目业务文件管理软件技术方案

**版本：** 0.1（MVP）

**日期：** 2026-09-23

## 1. 方案前提

本方案按首版在 Windows 电脑上运行、每台电脑独立保存项目与进度数据来设计。用户在本机选择项目资料的存放位置，应用负责创建项目目录、复制模板、归集附件，并记录每份文件的准备、填写、签字和归档状态。

首版不包含账号体系、跨电脑同步、多人同时编辑、云端存储和电子签章。状态由用户手动更新。

## 2. 推荐技术栈

| 层次 | 技术 | 用途 |
| --- | --- | --- |
| 界面 | Vue 3、TypeScript、Vite、Element Plus | 实现项目、模板、业务事项、文件清单和筛选页面 |
| Windows 桌面应用 | Electron、electron-vite | 提供桌面窗口、系统文件夹选择、应用生命周期及本地能力 |
| 本地数据 | SQLite、better-sqlite3 | 保存项目资料、模板规则、事项、清单状态和附件索引 |
| 文件操作 | Node.js 文件系统 API | 在用户授权的目录中创建文件夹、复制模板和附件、打开文件 |
| Windows 安装包 | electron-builder、NSIS | 生成可安装的 Windows 安装程序 |
| 自动化验证 | Vitest、临时 SQLite 数据库和临时文件夹 | 检查业务规则、数据持久化和文件操作 |

Vue 官方支持通过 Vite 创建 TypeScript 项目；Element Plus 提供 Vue 3 组件，适合表格、表单和筛选等管理界面。桌面开发可采用 electron-vite 组织主进程、预加载脚本和 Vue 界面。Electron 官方文档介绍了应用打包与 Windows 分发方式。[Vue 与 TypeScript](https://vuejs.org/guide/typescript/overview) [Element Plus 快速开始](https://element-plus.org/en-US/guide/quickstart) [electron-vite 快速开始](https://electron-vite.org/guide/) [Electron 分发说明](https://www.electronjs.org/docs/latest/tutorial/distribution-overview)

SQLite 是本地桌面应用的嵌入式数据库。better-sqlite3 可从 Node.js 访问 SQLite；由于它包含原生模块，构建安装包时要验证 Electron 版本、Windows 架构和原生模块的打包配置。[SQLite 适用场景](https://www.sqlite.org/whentouse.html) [better-sqlite3 项目说明](https://github.com/WiseLibs/better-sqlite3) [better-sqlite3 安装问题说明](https://github.com/WiseLibs/better-sqlite3/blob/master/docs/troubleshooting.md)

## 3. 应用结构与数据边界

应用分为三个部分：

1. **界面进程**：Vue 页面负责显示和编辑项目、事项及文件状态，不直接访问系统文件或数据库。
2. **预加载层**：向界面提供范围明确的接口，例如选择文件夹、创建项目、导入附件、更新状态和打开文件。
3. **主进程**：校验界面请求，执行 SQLite 读写与文件操作，并向界面返回结果或错误信息。

Electron 窗口关闭 Node.js 直接访问能力，启用上下文隔离和进程沙箱，只通过预加载层暴露所需接口。Electron 官方安全指南建议隔离界面与系统能力，并限制进程间通信的内容。[Electron 安全建议](https://www.electronjs.org/docs/latest/tutorial/security)

SQLite 数据库放在应用的本机数据目录中，不放入用户可移动的项目文件夹。数据库保存项目、客户信息、负责人、模板规则、业务事项、文件清单项、状态和文件索引。上传的模板原件保存在应用本机模板目录；生成的项目文件和附件则实际保存在用户指定的项目目录中。数据库记录项目根目录位置及项目内的相对文件路径。

建议围绕“项目、模板、业务事项、清单项、附件”建立数据表，并为关联关系设置外键。每次创建业务事项时，把所选模板的文件清单和完成要求复制为该事项自己的清单快照。以后修改模板时，不回头改变已创建事项的要求。

## 4. 关键实现方法

### 4.1 创建项目与复制模板

用户选择项目上级目录并填写项目信息。应用先校验项目名称和目录权限，再以安全的项目名称创建目录，将选中的模板文件复制到对应业务类别目录，最后保存项目记录。若创建或复制中途失败，应用提示失败原因并清理本次未完成的目录或文件，避免留下状态不明的半成品。

模板原件只读保留；项目中使用的是复制件。重名文件采用明确的冲突提示或自动改名策略，不静默覆盖已有资料。

### 4.2 创建业务事项与文件清单

用户在项目中选择事项类型、名称、日期和经办人。应用根据对应模板生成清单快照，每个清单项记录是否需要准备、填写、签字和归档，以及各步骤的状态。用户可针对本次事项调整清单；不适用的步骤不参与完成率计算。

项目总览和事项详情从数据库中的清单状态汇总未完成项。完成率只统计适用的必要步骤，并分别展示未准备、待填写、待签字和待归档数量。

### 4.3 附件导入与打开

附件通过系统文件选择窗口导入。应用将文件复制到对应项目和业务事项目录，保留来源文件不变；数据库保存附件原名、相对路径、所属清单项和导入时间。导入时处理文件名冲突，并检查目标目录是否可写。点击附件时，通过系统默认程序打开 Word、Excel、PDF 或图片文件。

项目文件夹被移动或文件被外部删除时，应用应提示路径失效，并允许重新定位或移除该附件记录，不把“数据库中有记录”误显示成“文件仍存在”。

### 4.4 数据持久化与迁移

项目、模板和状态更新通过数据库事务保存；文件复制成功后再登记附件记录。应用启动时执行版本化数据库迁移，确保不同版本升级时表结构有序变化。

每台电脑的数据相互独立。SQLite 官方建议，应用与共享数据位于不同设备时应考虑客户端服务器数据库，因此首版不应把本地 SQLite 数据库放到 NAS 或共享网络目录供多台电脑同时写入。[SQLite 适用场景](https://www.sqlite.org/whentouse.html)

由于数据库、模板原件和项目文件分开放置，后续应提供清晰的备份与恢复方式；迁移到另一台电脑时需一并迁移数据库及相关模板、项目目录，并恢复目录关联。

## 5. 实施顺序

1. 建立 Vue、TypeScript、Electron 项目骨架，配置安全的预加载接口和 Windows 开发运行流程。
2. 建立 SQLite 数据模型和数据库迁移机制，完成项目与模板的基础读写。
3. 实现目录选择、项目目录创建、模板复制和附件导入。
4. 实现业务事项、清单快照、状态更新、进度汇总和项目筛选。
5. 打包 Windows 安装程序，验证安装、升级、重启后的数据保留和错误提示。

现有静态网页 Demo 可作为界面和流程参考；真实文件复制、附件关联和持久化逻辑应放在 Electron 主进程及 SQLite 服务中实现。

## 6. 主要验证点

- 应用关闭并重新打开后，项目、事项和状态仍然存在。
- 修改模板不会改变此前创建的事项清单。
- 模板源文件不会被项目中的填写件覆盖。
- 中文目录、空格、重名文件、无权限目录和磁盘不可用时均有明确处理。
- 文件复制失败不会留下错误的附件记录或显示为已准备。
- 必要步骤、不适用步骤和完成率统计符合业务规则。
- 安装后的 Windows 程序能够读写本机数据库、调用系统文件选择窗口并打开关联文件。

## 7. 需要关注的限制

- 每台电脑保存独立数据，因此首版无法自动共享项目进度。
- 用户手动维护填写、签字和归档状态；首版不自动解析 Office 文件或扫描件。
- 本机数据库与项目文件夹是两部分数据，需明确备份和迁移方式。
- Electron 的安装包体积和内存占用高于纯网页，但可直接使用成熟的 Windows 文件系统能力。
